# Server-side (API routes)
GROQ_API_KEY=your_groq_api_key_here
PERFORMANCE_ADMIN_TOKEN=optional_admin_token

# Client-side (Vite exposes to browser, must start with VITE_)
VITE_APP_URL=http://localhost:3000
